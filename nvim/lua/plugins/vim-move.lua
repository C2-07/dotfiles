return {
    "matze/vim-move",
    event = "VeryLazy",
    init = function()
        -- Optional remaps (uncomment if you want to change defaults)
        -- vim.g.move_key_modifier = "C"  -- Change Alt to Ctrl
        -- vim.g.move_map_keys = 0       -- Disable default keybindings if you want custom ones
    end,
}
